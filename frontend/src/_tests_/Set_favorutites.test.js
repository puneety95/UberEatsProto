
import React from "react";
import { render, fireEvent } from '@testing-library/react'

import BackDrop from "../BackDrop";


it("Checking for the Restaurant dashboard in Customer persona", () => {
  const {queryByTitle} = render(<BackDrop/>);
  
})    

